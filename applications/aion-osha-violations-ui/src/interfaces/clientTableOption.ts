export interface IClientTableOption {
  id: string;
  associatedCompanyName?: string;
  citationId: string;
  activityNumber: React.ReactElement;
  openedDate: string;
  closedDate: string;
  violationType: string;
}
