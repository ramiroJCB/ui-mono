export interface IContractorTableOption {
  id: string;
  citationId: string;
  activityNumber: React.ReactElement;
  violationType: string;
  openedDate: string;
  closedDate: string;
  state: string;
}
